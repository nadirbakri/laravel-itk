version: '3.8'
services: 
    main:
        build: 
            context: .
            dockerfile: Dockerfile
        command: 'php artisan serve --host=0.0.0.0 --port=5000'
        volumes:
            - .:/app
        ports:
            - 5000:5000